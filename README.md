# Layout for Used Car Sales

**UAS Pemrograman Web**

- **Nama Mahasiswa**: Kevin Delvaro
- **NIM**: 251011750163

## Running the code

Run `pnpm install` to install the dependencies.

Run `pnpm dev` to start the development server.
